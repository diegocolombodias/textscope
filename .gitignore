# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local models and generated artifacts
models/
reference.json

# Downloaded source PDFs for the reference corpus (large binaries; the
# extracted .txt files under reference_corpus/txt/ are tracked instead)
reference_corpus/pdf/

# Original zip (source already extracted and tracked)
textscope.zip
