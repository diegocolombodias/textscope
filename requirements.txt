# Core (stylometry only) — no ML dependencies
numpy>=1.24

# Optional: local surprisal scoring
# torch>=2.0
# transformers>=4.40

# Optional: web UI
# gradio>=4.0

# Optional: training/using the black-box classifier (textscope/classifier.py)
# — read that module's docstring before using it
# datasets>=2.19
# scikit-learn>=1.3
# accelerate>=0.30
